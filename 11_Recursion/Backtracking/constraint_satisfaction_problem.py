# ------------------- CSP ------------------------- #
# Exploiting symmetry can be used to reduce combinatorial search
# Pruning away partial solutions identical to those previously considered

# --------------SUDOKU PROBLEM-------------------- #
# Given a partially filled grid of size n*n completely fill the grid with numbers between 1 and n
# Each row has all numbers from 1 to n
# Each column has all  numbers from 1 to n
# each subgrid (sqrt(n), sqrt(n)) has all numbers from 1 to n